{
  "name": "redirector",
  "version": "1.0.0",
  "description": "A simple URL redirect service using in-memory JSON file",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc && cp src/redirects.json dist/",
    "start": "node dist/app.js",
    "format": "prettier src --write"
  },
  "dependencies": {
    "express": "4.19.2"
  },
  "devDependencies": {
    "@types/express": "4.17.0",
    "prettier": "3.3.3",
    "typescript": "5.5.4"
  }
}
